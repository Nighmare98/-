<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
       Title="Ростелеком - Управление клиентами" Height="450" Width="800" Background="#002efe">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="5">
            <Button Content="Добавить" Width="100" Margin="5" Click="AddCustomer_Click" Background="#002efe" Foreground="White"/>
            <Button Content="Редактировать" Width="100" Margin="5" Click="EditCustomer_Click" Background="#002efe" Foreground="White"/>
            <Button Content="Удалить" Width="100" Margin="5" Click="DeleteCustomer_Click" Background="#002efe" Foreground="White"/>
            <TextBox Name="SearchBox" Width="200" Margin="5" TextChanged="SearchBox_TextChanged" 
                     VerticalContentAlignment="Center" Padding="5" Background="#002efe" Foreground="White"/>
        </StackPanel>

        <DataGrid Name="CustomersGrid" Grid.Row="1" Margin="5" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="*"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding PhoneNumber}" Width="120"/>
                <DataGridTextColumn Header="Услуга" Binding="{Binding ServiceType}" Width="120"/>
                <DataGridTextColumn Header="Абон. плата" Binding="{Binding MonthlyPayment}" Width="100"/>
                <DataGridCheckBoxColumn Header="Активен" Binding="{Binding IsActive}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Name="StatusText">Готово</TextBlock>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
